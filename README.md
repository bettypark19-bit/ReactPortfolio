
  # React App with CSS

  This is a code bundle for React App with CSS. The original project is available at https://www.figma.com/design/IGytVjhS4muqsE6IIzdVLN/React-App-with-CSS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  